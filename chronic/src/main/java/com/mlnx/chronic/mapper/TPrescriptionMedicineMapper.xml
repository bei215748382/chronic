<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mlnx.chronic.mapper.TPrescriptionMedicineMapper" >
  <resultMap id="BaseResultMap" type="com.mlnx.chronic.entity.TPrescriptionMedicine" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="prescription_id" property="prescriptionId" jdbcType="INTEGER" />
    <result column="medicine_id" property="medicineId" jdbcType="INTEGER" />
    <result column="sum" property="sum" jdbcType="VARCHAR" />
    <result column="already" property="already" jdbcType="VARCHAR" />
    <result column="isRemind" property="isremind" jdbcType="INTEGER" />
    <result column="dosage" property="dosage" jdbcType="VARCHAR" />
    <result column="eatStatuses" property="eatstatuses" jdbcType="VARCHAR" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    delete from t_prescription_medicine
    where id = #{id}
  </delete>
  <insert id="insert" parameterType="com.mlnx.chronic.entity.TPrescriptionMedicine" >
    insert into t_prescription_medicine (id, prescription_id, medicine_id, 
      sum, already, isRemind, 
      dosage, eatStatuses)
    values (#{id}, #{prescriptionId}, #{medicineId}, 
      #{sum}, #{already}, #{isremind}, 
      #{dosage}, #{eatstatuses})
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.mlnx.chronic.entity.TPrescriptionMedicine" >
    update t_prescription_medicine
    set prescription_id = #{prescriptionId},
      medicine_id = #{medicineId},
      sum = #{sum},
      already = #{already},
      isRemind = #{isremind},
      dosage = #{dosage},
      eatStatuses = #{eatstatuses}
    where id = #{id}
  </update>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    select id, prescription_id, medicine_id, sum, already, isRemind, dosage, eatStatuses
    from t_prescription_medicine
    where id = #{id}
  </select>
  <select id="selectAll" resultMap="BaseResultMap" >
    select id, prescription_id, medicine_id, sum, already, isRemind, dosage, eatStatuses
    from t_prescription_medicine
  </select>
</mapper>